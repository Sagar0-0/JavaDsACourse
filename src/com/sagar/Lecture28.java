package com.sagar;

public class Lecture28 {
    int num;
    public static void main(String[] args) {
        /*
        Public - everywhere (same + diff packages)
        Protected - Within same packages + in subclass of other packages
        Default - Within same package
        Private - Within same class
         */
    }
    static class MyClass{
        protected int value;
    }
}
